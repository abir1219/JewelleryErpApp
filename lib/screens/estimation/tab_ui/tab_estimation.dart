import 'package:flutter/material.dart';

class TabEstimation extends StatelessWidget {
  const TabEstimation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Center(child: Text("Tab Estimation"),)),
    );
  }
}
